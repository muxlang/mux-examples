# stream_interfaces

Use generic `Readable`, `Writable`, and `Seek` capability bounds with concrete
in-memory `Reader` and `Writer` handles. The example is deterministic and
offline, and demonstrates bounded reads, writes, and seeking.

```bash
mux run main.mux
```
