# stdlib_foundations

Exercise deterministic, offline standard-library building blocks: math,
bytes-native encoding, regular expressions, UUIDs, URLs, path operations,
cryptographic digests, explicit CLI parsing, logger configuration, and command
construction. Every input is fixed, so the recorded output is portable across
supported platforms; no command is executed and no log line is emitted.

```bash
mux run main.mux
```
